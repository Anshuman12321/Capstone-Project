## GitHub Pages / production
# Set this to your backend origin (no trailing slash).
# Example:
# VITE_API_ORIGIN=https://capstone-project-bananaball.onrender.com
VITE_API_ORIGIN=

# Auth/session storage mode: auto | session | local
# auto => sessionStorage in dev, localStorage in production
VITE_AUTH_STORAGE=auto

